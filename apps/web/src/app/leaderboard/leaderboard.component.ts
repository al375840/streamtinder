import { Component } from '@angular/core';

@Component({
  selector: 'app-leaderboard',
  standalone: true,
  template: `<div style="padding: 64px; font-family: var(--font-title); color: var(--c-bone);">LEADERBOARD (pendiente)</div>`
})
export class LeaderboardComponent {}
